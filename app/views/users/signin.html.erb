<%# Web page for user sign in %>
<% provide(:title, "Sign In") %>

<h2>User Sign In</h2>

<%= render "shared/flash" %>

<div id="main_layout">

  <%= form_tag(users_signin_path) do %>
    <div id="center_public_layout">
      Username:<br>
      <%= text_field_tag "user_name", nil, class: "text_field" %><br><br>
      Password:<br>
      <%= password_field_tag "password", nil, class: "text_field" %><br><br><br>
      <%= hidden_field_tag "client_time", "#{Time.now}" %>
    </div>

    <div id="bottom_layout">
      <%= submit_tag "Sign In", class: "button" %>
      <button class="button" onclick="location.href='/users/registration'; return false;">Register</button>
    </div>
  <% end %>

  <br><br>
  <p class="left_align_paragraph">The Savings Organizer application is a tool for people who desire to organize their savings account(s) into separate categories for which they are saving (e.g. emergency, travel, etc.); whereby the user can add (or deduct) dollar entries from each category, and track savings for different purposes to meet their saving goals.</p>
  <p class="left_align_paragraph">Please sign in or register to create a new user account.</p>

</div>
